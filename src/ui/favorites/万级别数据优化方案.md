
# Favorites 模块万级别数据优化方案

## 问题分析

### 当前问题

在万级别数据量时，Favorites 模块出现视频信息无法显示的问题，主要原因是：

1. **架构不一致**：Favorites 模块没有完全采用与 Stats 模块相同的 Book 模式架构
2. **索引缓存使用不当**：对整个索引缓存进行多次 filter 操作，性能低下
3. **数据加载策略问题**：可能一次性加载所有数据到内存，导致性能瓶颈

### Stats 模块的成功经验

Stats 模块能够流畅处理万级别数据，其关键设计：

1. **Book 模式**：
   - Book 只存储查询返回的索引 ID 列表
   - 不存储完整对象，减少内存占用
   - 通过 Repository 按需获取完整数据

2. **索引缓存无限制**：
   - IndexCache 不限制容量，满足全量查询要求
   - 查询时直接从索引缓存中筛选 ID

3. **分页加载**：
   - DataCache 只缓存当前页和预加载页的数据
   - 通过 Book 的 `loadPage` 方法按需加载完整数据

## 详细修改方案

### 1. 优化 FavoriteVideoQueryService.query 方法

**文件位置**：`src/database/query-server/query/favorite-video-query-service.ts`

**问题代码**（第33-106行）：
```typescript
async query(condition: FavoriteVideoQueryCondition): Promise<QueryOutput> {
  await this.ensureIndexCaches(condition.platform);

  // 问题：直接获取所有索引，然后进行多次 filter
  let results = this.favoriteVideoIndexCache.values().filter(index => index.platform === condition.platform);

  if (condition.collectionType) {
    results = results.filter(index => index.collectionTypes.includes(condition.collectionType!));
  }

  if (condition.collectionIds && condition.collectionIds.length > 0) {
    results = results.filter(index =>
      condition.collectionIds!.some(collectionId => index.collectionIds.includes(collectionId))
    );
  }

  // ... 更多 filter 操作
}
```

**优化方案**：
```typescript
async query(condition: FavoriteVideoQueryCondition): Promise<QueryOutput> {
  await this.ensureIndexCaches(condition.platform);

  // 优化1：使用 Map 结构加速查询
  const indexMap = new Map<number, FavoriteVideoIndex>();
  this.favoriteVideoIndexCache.values().forEach(index => {
    indexMap.set(index.favoriteEntryId, index);
  });

  // 优化2：一次性构建所有筛选条件
  const filters: ((index: FavoriteVideoIndex) => boolean)[] = [];

  // 平台筛选
  filters.push(index => index.platform === condition.platform);

  // 收藏夹类型筛选
  if (condition.collectionType) {
    filters.push(index => index.collectionTypes.includes(condition.collectionType!));
  }

  // 收藏夹ID筛选
  if (condition.collectionIds && condition.collectionIds.length > 0) {
    const collectionIdSet = new Set(condition.collectionIds);
    filters.push(index => 
      collectionIdSet.has(index.collectionIds.find(id => collectionIdSet.has(id))!)
    );
  }

  // 创作者筛选
  if (condition.creatorKeyword) {
    const creatorTerms = this.normalizeSearchTerms(condition.creatorKeyword);
    const creatorIdSet = new Set(
      this.creatorIndexCache.values()
        .filter(creator => this.matchesAllTerms(creator.name, creatorTerms))
        .map(creator => creator.creatorId)
    );
    filters.push(index => creatorIdSet.has(index.creatorId));
  }

  // 标签筛选
  if (condition.tagExpressions && condition.tagExpressions.length > 0) {
    const filterResult = this.filterByTags(
      Array.from(indexMap.values()), 
      condition.tagExpressions
    );
    const matchedIdSet = new Set(filterResult.map(index => index.favoriteEntryId));
    filters.push(index => matchedIdSet.has(index.favoriteEntryId));
  }

  // 关键词筛选
  if (condition.keyword) {
    const titleTerms = this.normalizeSearchTerms(condition.keyword);
    filters.push(index => this.matchesAllTerms(index.title, titleTerms));
  }

  // 优化3：应用所有筛选条件
  const results = Array.from(indexMap.values()).filter(index => 
    filters.every(filter => filter(index))
  );

  // 排序
  results.sort((left, right) => right.addedAt - left.addedAt);

  return {
    matchedIds: results.map(index => index.favoriteEntryId),
    stats: {
      initialCount: this.favoriteVideoIndexCache.size(),
      stageCounts: {}
    }
  };
}
```

### 2. 优化 FavoriteVideoRepository.getAll 方法

**文件位置**：`src/database/repositories/favorite-video-repository.ts`

**问题代码**（第66-144行）：
```typescript
async getAll(): Promise<FavoriteVideoEntry[]> {
  // 问题：如果缓存被清理，会重新加载所有数据
  if (this.dataCache.size() > 0) {
    return this.dataCache.values();
  }

  // 加载所有数据...
}
```

**优化方案**：
```typescript
async getAll(): Promise<FavoriteVideoEntry[]> {
  // 只在首次加载时构建完整数据
  if (this.indexCache.size() > 0 && this.dataCache.size() > 0) {
    // 返回已缓存的数据
    return this.dataCache.values();
  }

  // 首次加载：构建索引缓存
  const collections = await this.collectionRepo.getCollectionsByPlatform(Platform.BILIBILI);
  const collectionMap = new Map(
    collections.map(collection => [collection.collectionId, collection])
  );

  const itemsByCollection = await this.collectionRepo.getCollectionItemsByCollectionIds(
    collections.map(collection => collection.collectionId)
  );

  const aggregated = new Map<ID, AggregatedFavoriteItem>();
  itemsByCollection.forEach((items, collectionId) => {
    const collection = collectionMap.get(collectionId);
    if (!collection) return;

    items.forEach((item) => {
      const existing = aggregated.get(item.videoId);
      if (!existing) {
        aggregated.set(item.videoId, {
          latestItem: item,
          collectionIds: [collectionId],
          collectionNames: [collection.name],
          collectionTypes: [collection.type ?? "user"]
        });
        return;
      }

      existing.collectionIds.push(collectionId);
      existing.collectionNames.push(collection.name);
      if (!existing.collectionTypes.includes(collection.type ?? "user")) {
        existing.collectionTypes.push(collection.type ?? "user");
      }
      if (item.addedAt > existing.latestItem.addedAt) {
        existing.latestItem = item;
      }
    });
  });

  const videoIds = Array.from(aggregated.keys());
  const videos = await this.videoRepo.getVideos(videoIds);
  const creators = await this.creatorRepo.getCreators(
    Array.from(new Set(Array.from(videos.values()).map(video => video.creatorId)))
  );
  const tagIds = Array.from(new Set(Array.from(videos.values()).flatMap(video => video.tags)));
  const tags = await this.tagRepo.getTags(tagIds);

  await this.ensureTagIndexCache(tagIds);

  const entries: FavoriteVideoEntry[] = [];
  const cacheEntries = new Map<ID, FavoriteVideoEntry>();
  const indexEntries = new Map<ID, FavoriteVideoIndex>();

  videoIds.forEach((videoId) => {
    const video = videos.get(videoId);
    const aggregate = aggregated.get(videoId);
    if (!video || !aggregate || video.isInvalid) {
      return;
    }

    const creator = creators.get(video.creatorId);
    const entry = this.toEntry(video, creator ?? null, aggregate, tags);
    entries.push(entry);
    cacheEntries.set(entry.favoriteEntryId, entry);
    indexEntries.set(entry.favoriteEntryId, this.toIndex(entry));
  });

  // 批量设置缓存
  this.dataCache.setBatch(cacheEntries);
  this.indexCache.setBatch(indexEntries);

  return entries;
}
```

### 3. 优化 services.ts 中的数据加载逻辑

**文件位置**：`src/ui/favorites/services.ts`

**问题代码**（第167-195行）：
```typescript
async queryVideos(query: FavoriteVideoQuery): Promise<FavoriteVideoQueryResult> {
  const condition = this.toQueryCondition(query);
  const page = await this.getQueryPage(condition, query.page, query.pageSize);

  return {
    items: page.items.map(entry => this.toVideoListItem(entry)),
    total: page.state.totalRecords,
    page: page.state.currentPage,
    pageSize: page.state.pageSize,
    totalPages: Math.max(1, page.state.totalPages || 1),
    availableCollectionIds: this.getSelectedCollectionIds(query.collectionType, query.selectedCollectionId)
  };
}
```

**优化方案**：
```typescript
async queryVideos(query: FavoriteVideoQuery): Promise<FavoriteVideoQueryResult> {
  const condition = this.toQueryCondition(query);

  // 确保索引缓存已加载
  await this.favoriteVideoQueryService.loadIndexCache(condition.platform);

  // 获取查询结果
  const page = await this.getQueryPage(condition, query.page, query.pageSize);

  // 优化：只转换当前页的数据
  const items = page.items.map(entry => this.toVideoListItem(entry));

  return {
    items,
    total: page.state.totalRecords,
    page: page.state.currentPage,
    pageSize: page.state.pageSize,
    totalPages: Math.max(1, page.state.totalPages || 1),
    availableCollectionIds: this.getSelectedCollectionIds(query.collectionType, query.selectedCollectionId)
  };
}
```

### 4. 优化 FavoritesManager 中的渲染逻辑

**文件位置**：`src/ui/favorites/FavoritesManager.ts`

**问题代码**（第341-411行）：
```typescript
private async renderVideos(): Promise<void> {
  // 问题：每次渲染都创建新的 RenderBook
  const renderBook = await this.dataService.getRenderBook(query, new FavoriteListElementBuilder());

  if (!this.favoriteListRender) {
    this.favoriteListRender = new FavoriteListRender({
      container: list,
      renderBook,
      autoRender: false,
      onPageChange: (page) => {
        this.state.currentPage = page;
      }
    });
    await this.favoriteListRender.initialize(safePage);
  } else {
    await this.favoriteListRender.renderPage(safePage, { pageSize: this.state.pageSize });
  }
}
```

**优化方案**：
```typescript
private async renderVideos(): Promise<void> {
  const list = document.getElementById("videoList");
  const empty = document.getElementById("empty");
  const loading = document.getElementById("loading");

  if (!list || !empty) {
    return;
  }

  if (this.state.error) {
    return;
  }

  const query = {
    collectionType: this.state.collectionType,
    selectedCollectionId: this.state.selectedCollectionId,
    keyword: this.state.searchKeyword,
    includeTagIds: this.state.includeTagIds,
    excludeTagIds: this.state.excludeTagIds,
    page: this.state.currentPage,
    pageSize: this.state.pageSize
  };

  loading?.style.setProperty("display", "none");

  // 优化：复用 RenderBook 实例
  const renderBook = await this.dataService.getRenderBook(query, new FavoriteListElementBuilder());
  const total = renderBook.state.totalRecords;
  const totalPages = Math.max(1, renderBook.state.totalPages || 1);
  const safePage = renderBook.state.totalPages === 0
    ? 0
    : Math.min(this.state.currentPage, renderBook.state.totalPages - 1);

  this.state.currentPage = safePage;

  if (total === 0) {
    list.innerHTML = "";
    empty.style.display = "flex";
    const pagination = document.getElementById("pagination");
    if (pagination) {
      pagination.style.display = "none";
    }
  } else {
    empty.style.display = "none";

    // 优化：只创建一次 RenderBook
    if (!this.favoriteListRender) {
      this.favoriteListRender = new FavoriteListRender({
        container: list,
        renderBook,
        autoRender: false,
        onPageChange: (page) => {
          this.state.currentPage = page;
          if (FavoritesManager.DEBUG) {
            console.log("[FavoritesManager] page changed:", { page });
          }
        }
      });
      await this.favoriteListRender.initialize(safePage);
    } else {
      // 更新 Book 的索引
      await renderBook.updateIndex(this.dataService.toQueryCondition(query));
      await this.favoriteListRender.renderPage(safePage, { pageSize: this.state.pageSize });
    }
  }
}
```

### 5. 优化图片加载策略

**文件位置**：`src/ui/favorites/FavoriteListElementBuilder.ts`

**问题代码**（第116-140行）：
```typescript
private async applyCoverSource(image: HTMLImageElement, item: FavoriteVideoEntry): Promise<void> {
  try {
    // 问题：每个视频卡片都异步加载封面
    const cachedUrl = this.coverUrlCache.get(item.videoId);
    if (cachedUrl) {
      image.src = cachedUrl;
      return;
    }

    if (item.picture) {
      const coverBlob = await this.videoRepository.getVideoPicture(item.videoId);
      if (coverBlob) {
        const objectUrl = URL.createObjectURL(coverBlob);
        this.coverUrlCache.set(item.videoId, objectUrl);
        image.src = objectUrl;
        return;
      }
    }
  } catch (error) {
    console.error(`[FavoriteListElementBuilder] 加载本地封面失败 (videoId: ${item.videoId}):`, error);
  }

  this.applyRemoteCoverFallback(image, item.coverUrl);
}
```

**优化方案**：
```typescript
private readonly coverUrlCache = new Map<ID, string>();
private readonly loadingCovers = new Set<ID>();
private readonly coverQueue = new Array<{ videoId: ID; image: HTMLImageElement; item: FavoriteVideoEntry }>();
private isProcessingQueue = false;

private async applyCoverSource(image: HTMLImageElement, item: FavoriteVideoEntry): Promise<void> {
  try {
    // 检查 URL 缓存
    const cachedUrl = this.coverUrlCache.get(item.videoId);
    if (cachedUrl) {
      image.src = cachedUrl;
      return;
    }

    // 检查是否正在加载
    if (this.loadingCovers.has(item.videoId)) {
      // 添加到队列
      this.coverQueue.push({ videoId: item.videoId, image, item });
      return;
    }

    // 标记为正在加载
    this.loadingCovers.add(item.videoId);

    if (item.picture) {
      try {
        const coverBlob = await this.videoRepository.getVideoPicture(item.videoId);
        if (coverBlob) {
          const objectUrl = URL.createObjectURL(coverBlob);
          this.coverUrlCache.set(item.videoId, objectUrl);
          image.src = objectUrl;

          // 处理队列中的相同请求
          this.processCoverQueue(item.videoId, objectUrl);
          return;
        }
      } catch (error) {
        console.error(`[FavoriteListElementBuilder] 加载本地封面失败 (videoId: ${item.videoId}):`, error);
      } finally {
        this.loadingCovers.delete(item.videoId);
      }
    }

    this.applyRemoteCoverFallback(image, item.coverUrl);
  } catch (error) {
    console.error(`[FavoriteListElementBuilder] 加载封面失败 (videoId: ${item.videoId}):`, error);
    this.loadingCovers.delete(item.videoId);
  }
}

private processCoverQueue(videoId: ID, objectUrl: string): void {
  const pendingItems = this.coverQueue.filter(item => item.videoId === videoId);
  pendingItems.forEach(({ image }) => {
    image.src = objectUrl;
  });
  this.coverQueue = this.coverQueue.filter(item => item.videoId !== videoId);
}
```

## 实施步骤

### 第一阶段：核心优化
1. 优化 `FavoriteVideoQueryService.query` 方法
2. 优化 `FavoriteVideoRepository.getAll` 方法
3. 测试万级别数据量的查询性能

### 第二阶段：渲染优化
1. 优化 `services.ts` 中的数据加载逻辑
2. 优化 `FavoritesManager` 中的渲染逻辑
3. 测试页面渲染性能

### 第三阶段：图片优化
1. 优化图片加载策略
2. 实现图片缓存和队列管理
3. 测试图片加载性能

## 预期效果

1. **查询性能**：
   - 索引查询时间从 O(n*m) 降低到 O(n)
   - 万级别数据量下查询响应时间 < 100ms

2. **内存占用**：
   - 完整数据对象缓存限制在 1000 条
   - 索引缓存无限制，但内存占用可控

3. **渲染性能**：
   - 首屏渲染时间 < 500ms
   - 页面切换时间 < 200ms
   - 图片加载不阻塞主线程

4. **用户体验**：
   - 支持万级别数据量的流畅操作
   - 分页、筛选、搜索响应迅速
   - 无卡顿现象

## 注意事项

1. **缓存一致性**：
   - 确保索引缓存和数据缓存的同步更新
   - 数据变更时及时清理相关缓存

2. **内存管理**：
   - 监控内存占用，避免内存泄漏
   - 及时释放不再使用的资源

3. **性能监控**：
   - 添加性能指标收集
   - 记录关键操作的耗时

4. **向后兼容**：
   - 确保修改不影响现有功能
   - 保持 API 接口不变
